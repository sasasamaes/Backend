query testInvalidWalletAddress($address: String!, $chainType: ChainType!) {
  validateWallet(address: $address, chainType: $chainType) {
    isValid
    errors {
      code
      message
      details
    }
    validation {
      formatValid
      checksumValid
      chainTypeValid
    }
  }
}